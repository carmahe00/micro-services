apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-mongo-depl
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-mongo
  template: ## configuration about of pod
    metadata:
      labels:
        app: auth-mongo
    spec:
      containers:
      - name: auth-mongo
        image: mongo

---
apiVersion: v1
kind: Service
metadata:
  name: auth-mongo-srv
spec:
  selector:
    app: auth-mongo
  ports:
  - port: 27017
    targetPort: 27017
    name: name
    protocol: TCP