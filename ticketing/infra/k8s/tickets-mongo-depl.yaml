apiVersion: apps/v1
kind: Deployment
metadata:
  name: tickets-mongo-depl
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tickets-mongo
  template: ## configuration about of pod
    metadata:
      labels:
        app: tickets-mongo
    spec:
      containers:
      - name: tickets-mongo
        image: mongo

---
apiVersion: v1
kind: Service
metadata:
  name: tickets-mongo-srv
spec:
  selector:
    app: tickets-mongo
  ports:
  - port: 27017
    targetPort: 27017
    name: name
    protocol: TCP